program: -m qensemble.main
name: cifar10_phase2_dependent
method: grid
metric:
  name: test/sparse_acc
  goal: maximize
parameters:
  model.variant:
    values: [medium, large]
  quant.activation_total_bits:
    values: [4, 6, 8]
  quant.activation_int_bits:
    value: 1
  quant.weight_total_bits:
    values: [4, 6, 8]
  quant.weight_int_bits:
    value: 1
  ensemble.size:
    values: [2, 3, 4, 5]
  run.seed:
    values: [0, 1]
command:
  - ${env}
  - python
  - ${program}
  - --config
  - configs/cifar10/base.yaml
