run:
  seed: 0
  out_root: outputs
  name: mnist-base

data:
  source: tf_keras
  dataset_name: mnist
  batch_size: 128
  val_split: 0.1

model:
  name: mlp
  width: [64, 64]
  num_classes: 10

quant:
  activation_total_bits: 8
  activation_int_bits: 3
  weight_total_bits: 8
  weight_int_bits: 2

train:
  epochs: 20
  optimizer: adam
  lr: 0.0003
  beta1: 0.9
  beta2: 0.999
  loss: sparse_ce
  metrics: [sparse_acc]

callbacks:
  early_stopping:
    enabled: true
    patience: 5
  checkpoint:
    enabled: true
    monitor: val/sparse_acc
  wandb:
    enabled: true 
    log_freq: epoch

wandb:
  enabled: true 
  project: qensemble
  group: mnist
  tags: ["mnist"]
